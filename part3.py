import torch
import numpy as np
import matplotlib.pyplot as plt

def sierpinski_arrowhead_curve(order -> int, length -> int) -> None:
    if order % 2 == 0:
        # curve(order, length, +60)
        return
    else:
        # curve(order, length, -60)
        return

